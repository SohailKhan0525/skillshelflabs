import Link from "next/link";
import { supabase } from "../../../lib/supabase";
export default async function Skill({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const {data:s}=await supabase.from("skills").select("slug,name,description,category,ai_tool,creator_name,organization_name,source_url,demo_url,instructions").eq("slug",slug).eq("status","published").maybeSingle();if(!s)return <main><nav className="nav"><Link href="/" className="brand">SKILLSHELF</Link><Link href="/skills">All skills</Link></nav><section className="page-head shell"><p className="eyebrow">NOT FOUND</p><h1>That skill is not in the library.</h1><Link href="/skills" className="button">Browse skills</Link></section></main>;
return <main><nav className="nav"><Link href="/" className="brand">SKILLSHELF</Link><Link href="/skills">All skills</Link></nav><section className="skill-hero shell"><p className="eyebrow">{s.ai_tool} · {s.category}</p><h1>{s.name}</h1><p>{s.description}</p><div className="actions"><Link className="button" href={s.demo_url||("/skills/"+slug+"/demo")}>See the demo</Link>{s.source_url&&<a className="text-link" href={s.source_url} target="_blank" rel="noreferrer">Read source</a>}</div></section><section className="shell content-grid"><article><p className="eyebrow">START HERE</p><h2>What does this skill actually do?</h2><p>{s.description}</p><p><strong>How do I use it?</strong> {s.instructions||"Follow the instructions provided by the skill creator."}</p><h2>See it in action</h2><p>The demo is a real SkillShelf page using the published skill information, not fabricated product metrics.</p><p><Link href={s.demo_url||("/skills/"+slug+"/demo")} className="text-link">Open the demo</Link></p><h2>Creator and source</h2><p><strong>{s.creator_name||"Community creator"}</strong>{s.organization_name?<> · {s.organization_name}</>:null}</p>{s.source_url&&<p><a href={s.source_url} target="_blank" rel="noreferrer" className="text-link">Open canonical source</a></p>}</article><aside><span>AI TOOL</span><strong>{s.ai_tool}</strong><span>CATEGORY</span><strong>{s.category}</strong><span>CREATOR</span><strong>{s.creator_name||"Community creator"}</strong><span>ORGANIZATION</span><strong>{s.organization_name||"—"}</strong></aside></section></main>}